{
  "name": "trypanosomatics-website",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "description": "Trypanosomatics Lab website - Hugo + Hugo Blox Kit",
  "scripts": {
    "dev": "hugo server --disableFastRender",
    "dev:search": "hugo --gc && pagefind --site public --output-path static/pagefind && hugo server --disableFastRender",
    "build": "hugo --gc --minify && pnpm run pagefind",
    "pagefind": "pagefind --site public",
    "clean:search": "rm -rf static/pagefind"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "@tailwindcss/typography": "^0.5.10",
    "pagefind": "^1.4.0",
    "preact": "^10.27.2",
    "tailwindcss": "^4.1.12"
  }
}
