+++
title = "Publications"

# List view for this section. Hugo Blox view names replace the old
# numeric ids (1=List, 2=Compact, 3=Card, 4=Citation).
view = "citation"

# Optional featured image (relative to `static/img/` folder).
# Reading time is gated on a per-page param, not a site setting, so it is
# cascaded to every page in this section. These pages are metadata with no
# body, and were rendering "0 min read" under the title.
# Must sit ABOVE [header]: a TOML table header absorbs every key after it.
cascade.reading_time = false

[header]
image = ""
caption = ""

+++
